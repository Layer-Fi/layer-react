// Must match $Layer--tax-details--min-desktop-width in ./_breakpoints.scss so the JS
// subtree swap and the CSS container query flip at the same boundary.
export const TAX_DETAIL_MIN_DESKTOP_WIDTH = 721
